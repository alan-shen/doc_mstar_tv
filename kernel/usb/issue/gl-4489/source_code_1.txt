----------------------------------------------------------------------------------------------------------
5892 int usb_hub_init(void)

	5900     khubd_task = kthread_run(hub_thread, NULL, "khubd");

----------------------------------------------------------------------------------------------------------
5757 static int hub_thread(void *__unused)
----------------------------------------------------------------------------------------------------------
5342 static void hub_events(void)

	5629 hub_port_connect_change(hub, i, portstatus, portchange);

----------------------------------------------------------------------------------------------------------
4962 static void hub_port_connect_change(struct usb_hub *hub, int port1, u16 portstatus, u16 portchange)

	status = hub_port_init(hub, udev, port1, i);

	OR--------------------------------------------------------------------------------------------------------
	6029 static int usb_reset_and_verify_device(struct usb_device *udev)
		ret = hub_port_init(parent_hub, udev, port1, i);

----------------------------------------------------------------------------------------------------------
4426 static int hub_port_init (struct usb_hub *hub, struct usb_device *udev, int port1, int retry_counter)

	4483     retval = hub_port_reset(hub, port1, udev, delay, false);
		3070             status = hub_port_wait_reset(hub, port1, udev, delay,warm);
			2854     printk("[USB %x], portstatus: 0x%x, portchange: 0x%x\n", hcd->ehc_base, portstatus, portchange);
----------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------
